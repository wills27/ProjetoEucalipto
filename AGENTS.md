# Agent Notes

Before navigating this project, read `CODE_INDEX.md`.

The project is a PyQt6 Cellpose workflow app. To reduce token use:

- Start from `CODE_INDEX.md` for module ownership and likely edit targets.
- Use `rg` for exact symbols before opening large files.
- Prefer service files for pure behavior and presenter/workflow files for UI actions.
- Open large UI files only after identifying the relevant method or class.

